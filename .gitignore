# secrets / local
.env
*.pyc
__pycache__/
bot_stats.db

# deps
node_modules/
gateway/

# promo local (deferred — do not ship auto-poster)
promo.py
promo_targets.json
promo_targets.example.json
promo_outbox/
